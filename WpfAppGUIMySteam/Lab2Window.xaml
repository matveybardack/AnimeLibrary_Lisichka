<Window x:Class="WpfAppGUIMySteam.Lab2Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ЛР-2: Движок слабейшего предусловия"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#0f3460">

    <Window.Resources>
        <!-- Стили в японском стиле -->
        <Style x:Key="AnimeCard" TargetType="Border">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15"/>
        </Style>

        <Style x:Key="AnimeButton" TargetType="Button">
            <Setter Property="Background" Value="#e94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="CodeBox" TargetType="TextBox">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ConditionBox" TargetType="TextBox">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="25"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Фон -->
        <Image Source="/Images/yakuza-0-goro-majima-kiryu-kazuma.jpg" 
               Stretch="UniformToFill" Opacity="0.3"/>

        <DockPanel>
            <!-- Заголовок -->
            <Border DockPanel.Dock="Top" 
                    Background="#1a1a2e" 
                    Padding="15" 
                    BorderThickness="0,0,0,2" 
                    BorderBrush="#e94560">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Назад" 
                            Style="{StaticResource AnimeButton}"
                            Command="{Binding BackCommand}"
                            Background="#16213e"
                            Grid.Column="0"/>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="WP エンジン - アニメ図書館" 
                                   Foreground="White" 
                                   FontSize="16" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="ЛР-2: Движок слабейшего предусловия для библиотеки аниме" 
                                   Foreground="#f8c3af" 
                                   FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Основной контент -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Левая колонка - ввод -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">

                        <!-- Блок выбора пресета -->
                        <Border Style="{StaticResource AnimeCard}" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="📋 Примеры алгоритмов библиотеки аниме" 
                                           Foreground="#e94560" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Margin="0,0,0,10"/>

                                <ComboBox ItemsSource="{Binding Presets}"
                                          SelectedItem="{Binding SelectedPreset}"
                                          DisplayMemberPath="Name"
                                          Background="#16213e" 
                                          Foreground="White" 
                                          BorderBrush="#e94560"
                                          Height="25"/>

                                <TextBlock Text="{Binding SelectedPreset.Description}" 
                                           Foreground="#cccccc" 
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Блок постусловия -->
                        <Border Style="{StaticResource AnimeCard}" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="🎯 Цель (Post-условие)" 
                                           Foreground="#e94560" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Margin="0,0,0,10"/>

                                <TextBlock Text="Формальная запись:" 
                                           Foreground="#cccccc" 
                                           FontSize="12"/>
                                <TextBox Style="{StaticResource ConditionBox}"
                                         Text="{Binding PostCondition}"
                                         IsReadOnly="True"/>

                                <TextBlock Text="Описание на русском:" 
                                           Foreground="#cccccc" 
                                           FontSize="12"
                                           Margin="0,5,0,0"/>
                                <TextBox Style="{StaticResource ConditionBox}"
                                         Text="{Binding PostDescription}"
                                         IsReadOnly="True"/>
                            </StackPanel>
                        </Border>

                        <!-- Блок программы -->
                        <Border Style="{StaticResource AnimeCard}">
                            <StackPanel>
                                <TextBlock Text="📝 Программа" 
                                           Foreground="#e94560" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Margin="0,0,0,10"/>

                                <TextBlock Text="Поддерживаются: присваивание (x := expr), if (условие) { ... } else { ... }, последовательность операторов" 
                                           Foreground="#cccccc" 
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,5"/>

                                <TextBox Style="{StaticResource CodeBox}"
                                         Text="{Binding ProgramCode}"
                                         Height="200"
                                         TextWrapping="Wrap"
                                         IsReadOnly="True"/>

                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="Рассчитать WP" 
                                            Style="{StaticResource AnimeButton}"
                                            Command="{Binding CalculateWpCommand}"/>
                                    <Button Content="Показать триаду" 
                                            Style="{StaticResource AnimeButton}"
                                            Background="#16213e"
                                            Command="{Binding ShowTriadCommand}"
                                            Margin="10,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                    </StackPanel>

                    <!-- Правая колонка - результаты -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">

                        <!-- Блок пошагового расчета -->
                        <Border Style="{StaticResource AnimeCard}" Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="🔍 Пошаговый расчет WP" 
                                           Foreground="#e94560" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Margin="0,0,0,10"/>

                                <ItemsControl ItemsSource="{Binding CalculationSteps}"
                                              Background="#16213e"
                                              BorderBrush="#e94560"
                                              BorderThickness="1"
                                              Padding="5"
                                              Height="250"
                                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#1a1a2e" 
                                                    Margin="1" 
                                                    Padding="8" 
                                                    CornerRadius="3">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding StepNumber}" 
                                                               Foreground="#e94560" 
                                                               FontSize="12"
                                                               FontWeight="Bold"
                                                               Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding Description}" 
                                                               Foreground="White" 
                                                               FontSize="12"
                                                               Grid.Column="1"
                                                               TextWrapping="Wrap"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Блок результатов -->
                        <Border Style="{StaticResource AnimeCard}">
                            <StackPanel>
                                <TextBlock Text="📊 Результаты" 
                                           Foreground="#e94560" 
                                           FontWeight="Bold" 
                                           FontSize="14"
                                           Margin="0,0,0,10"/>

                                <TextBlock Text="Слабейшее предусловие (WP):" 
                                           Foreground="#cccccc" 
                                           FontSize="12"/>
                                <TextBox Style="{StaticResource CodeBox}"
                                         Text="{Binding WeakestPrecondition}"
                                         Height="60"
                                         IsReadOnly="True"
                                         Background="#1a1a2e"/>

                                <TextBlock Text="Описание предусловия:" 
                                           Foreground="#cccccc" 
                                           FontSize="12"
                                           Margin="0,5,0,0"/>
                                <TextBox Style="{StaticResource CodeBox}"
                                         Text="{Binding PreDescription}"
                                         Height="40"
                                         IsReadOnly="True"
                                         Background="#1a1a2e"/>

                                <TextBlock Text="Триада Хоара:" 
                                           Foreground="#cccccc" 
                                           FontSize="12"
                                           Margin="0,5,0,0"/>
                                <TextBox Style="{StaticResource CodeBox}"
                                         Text="{Binding HoareTriad}"
                                         Height="60"
                                         IsReadOnly="True"
                                         Background="#1a1a2e"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>